version: '3.8'

services:
  myapp:
    build: .
    ports:
      - "5000:80"
    environment:
      AppSettings__SecretKey: ${SECRET_KEY}
      AppSettings__ConnectionStrings__DefaultDb: ${DB_CONNECTION}

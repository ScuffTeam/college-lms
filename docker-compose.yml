version: '3.8'

services:
  myapp:
    build: .
    ports:
      - "5000:80"
    environment:
      AppOptions__SecretKey: ${SECRET_KEY}
      AppOptions__ConnectionStrings__DefaultDb: ${DB_CONNECTION}
